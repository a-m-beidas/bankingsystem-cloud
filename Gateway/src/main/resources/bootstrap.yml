spring:
  cloud:
    config:
      fail-fast: true
      retry:
        max-interval: 1000
        max-attempts: 4
  application:
    name: gateway
    cloud:
      config:
        uri: http://localhost:8888