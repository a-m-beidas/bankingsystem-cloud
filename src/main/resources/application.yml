spring:
  jpa:
    hibernate:
      ddl-auto: create
      order_inserts: true
      order_updatest: true
      jdbc:
        batch_size: 4
    generate-ddl: true
    show-sql: true
  datasource:
    url: jdbc:mysql://localhost:3306/test4
    username: root
    password: root