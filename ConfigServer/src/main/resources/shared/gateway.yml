ribbon:
  ReadTimeout : 60000
  eureka:
    enabled: true
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
zuul:
  routes:
    banking:
      path : /**
      serviceId: banking
      stripPrefix: false
server:
  port: 80